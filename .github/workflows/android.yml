name: Unit Tests

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: chmod +x ./gradlew
      - uses: actions/setup-java@v2
        with:
          java-version: 8
          distribution: 'zulu'
      - run: ./gradlew build
